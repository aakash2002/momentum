import AIBreakdown from '@/components/screens/AIBreakdown';
import TransitionWrapper from "@/components/TransitionWrapper";

export default function PlanPage() {
  return (
    <TransitionWrapper>
        <AIBreakdown />
    </TransitionWrapper>
    );
}